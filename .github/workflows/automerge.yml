---
name: Auto merge
on:
  pull_request:
jobs:
  auto-merge:
    uses: clouddrove/github-shared-workflows/.github/workflows/auto_merge.yml@1.2.1
    secrets:
      GITHUB: ${{ secrets.D_GITHUB }}
    with:
      tfcheck: 'tf-static-checks / Check code format'
...
